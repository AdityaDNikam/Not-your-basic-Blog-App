Day1
.Created a lander, log_in, create_acc page
.inetiated a mongo schema form the basic creadentials

Day2
.Create the blogs showing page that also have Your profile related info too
.create the Update and delete routes too
.Update the UI part
.Set up cookies and authentecation on the "/blog/:user" route that shows the blogs

Day 3 
.Start learning React and start changing the ejs files to react files

Day 4
.Replace Ejs files with React 
.Checkout Claud API call integration.

Future plans 
Make a spend smarter web application, integrate it with this Blog app 
Make this blog app more of as task tracker app
add third party auth tools to it , add AI features to it.